App.Drawing.draw.fill(
  x0: I32,
  y0: I32,
  x1: I32,
  y1: I32,
  z:  I32,
  size: U32
  col: Col32,
  img: Image
): Image
  pos = Pos32.new(I32.to_u32(x1), I32.to_u32(y1), I32.to_u32(z))
  Image.fill(pos, col, img)

