App.Unlockind.draw: App.Draw<App.Unlockind.State>
(state)
  game = state@local@game
  case game { 
    select:
      App.Unlockind.Draw.select

    lobby:
      App.Unlockind.Draw.lobby(game.play)
    }
  
