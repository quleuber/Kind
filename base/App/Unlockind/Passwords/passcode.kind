// Gerador de senha
App.Unlockind.Passwords.passcode: IO<List<Nat>>
  IO {
    get num = IO.random(10)
    get num_1 = App.Unlockind.Passwords.list(4, 10)
    let lista = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
    let senha = App.Unlockind.Passwords.index_list(num_1, lista, [])
    return senha
  }
