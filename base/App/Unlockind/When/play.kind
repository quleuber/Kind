App.Unlockind.When.play(
  state: App.Unlockind.State.local
  try: Nat
): List<String>

  let list = IO { 
    get password = App.Unlockind.Passwords.passcode
    let user_input = state@game
    let user_input = case user_input { 
      lobby:
         []
      play: 
        user_input.input
    }
    let user_nums = App.Unlockind.Passwords.read(user_input)
    let user_try = App.Unlockind.String.answer(password, user_nums, password)
    ?c
    //let user_input = 
  }
  ?a
